export const numberToBinaryString = (num) => num.toString(2);

export const numberToHexString = (num) => num.toString(16);

export const onesComplement = (num) => ~num;

export const twosComplement = (num) => ~num + 1;
